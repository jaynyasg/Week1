# clinical-chat-ui

React + Vite UI for the clinical agent chat.

Build: `npm install && npm run build`. Dev: `npm run dev`. Preview: `npm run preview`.

Environment variables are documented in `../.env.example`; use `VITE_AGENT_BASE_URL` and optional `VITE_EMBEDDED` in a `.env` file next to this package.
